# Global Default Subscriptions

The global instance subscribes to all event types by default. It listens for registrations, updates, and queries. It acts as the central hub that sees everything on the bus.
