# Source Configuration UI

Build a UI for configuring ingestion sources — credentials (Slack tokens, GitHub tokens, etc.), channel/repo selection, and subscription settings. Since ingestion is centralized at the global orchestrator, this UI manages what the global instance ingests and which children receive what data.
