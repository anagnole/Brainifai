# Global Instance Init

Implement `brainifai init` when run outside a project. Creates the global Brainifai instance at ~/.brainifai/ with its own Kuzu DB. This is the root of the instance tree. Should only be created once.
