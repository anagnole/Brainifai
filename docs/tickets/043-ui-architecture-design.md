# UI Architecture Design

Design the unified web app architecture. Consolidate or replace the existing Next.js admin dashboard and Sigma.js graph visualization into one cohesive application. Decide on the framework, routing, and how the UI communicates with the backend (REST, WebSocket, etc.). Consider whether to build on top of the existing Next.js app or start fresh.
