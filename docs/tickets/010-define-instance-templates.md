# Define Instance Templates

Define predefined instance templates that bootstrap common configurations. Initial types: coding project (GitHub + Claude sessions, code-focused context), manager (Slack + Calendar + ClickUp, people-focused context), general (broad, everything). Each template should define default source subscriptions and context functions.
