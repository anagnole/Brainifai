# Build CLI

Build the brainifai CLI tool. This is the primary interface for managing instances. Should support subcommands for init, status, listing instances, and future extensibility. Choose a CLI framework (e.g., Commander.js).
