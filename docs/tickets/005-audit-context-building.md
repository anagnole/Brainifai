# Audit Context Building

Conceptual audit of context building functions (get_context_packet, search_entities, get_entity_summary, get_recent_activity). Evaluate whether the output is actually useful for AI sessions. Document what works and what doesn't.
