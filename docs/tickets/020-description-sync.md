# Description Sync

When a child instance's description is updated (either by user or AI session), the corresponding node in the parent's graph should be updated too. Define the mechanism for keeping these in sync.
