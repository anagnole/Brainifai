# Child Registration Flow

When a child instance is created via CLI, it registers itself with the global instance by creating a node in the global graph. This should happen automatically during `brainifai init` inside a project.
