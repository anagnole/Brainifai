# Extract ClickUp MCP Server

Extract the ClickUp MCP server (src/mcp-clickup/) from the Brainifai repo into its own standalone repository. It should work independently with its own package.json and README.
