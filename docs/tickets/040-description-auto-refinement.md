# Description Auto-Refinement

As an instance accumulates data over time, the AI session working in that project should automatically refine the instance's description to better reflect what the instance actually contains. This keeps the orchestrator's routing accurate.
