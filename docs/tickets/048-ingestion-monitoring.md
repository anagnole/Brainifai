# Ingestion Monitoring

Provide a dashboard for monitoring ingestion. Show last sync timestamps per source, trigger manual ingestion runs, stream live logs during ingestion, and display per-source status (success, error, skipped). Similar to the existing ingest page in the Next.js admin dashboard but adapted for the multi-instance model.
