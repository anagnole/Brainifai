# Configurable Context Functions

Allow instances to add or remove context functions beyond their template defaults. An instance should be able to opt into functions from other templates or define entirely new ones.
