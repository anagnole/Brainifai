# Graph Explorer

Build a graph explorer that lets users drill into any Brainifai instance and visually explore its knowledge graph — nodes (people, topics, containers, activities), edges, and relationships. Build on or replace the existing Sigma.js visualization. Support search, filtering, and navigation within the graph.
