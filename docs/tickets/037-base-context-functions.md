# Base Context Functions

Define the base context functions that every Brainifai instance ships with regardless of type. These are: entity search, recent activity feed, context packet generation, and memory ingestion. Port and adapt these from the current implementation.
