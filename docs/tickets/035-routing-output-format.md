# Routing Output Format

Define the structured output format the orchestrator uses for routing decisions. Something like: { "target": ["aballos", "global"], "data": { ... } }. This format should be parseable and actionable by the event bus.
