# Tree Visualization

Build an interactive visualization of the full Brainifai instance tree. Show the hierarchy from global instance down to project-level children. Display instance types, descriptions, status, and last sync timestamps. Allow clicking into any instance to explore it further.
