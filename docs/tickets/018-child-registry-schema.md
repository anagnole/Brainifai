# Child Registry Schema

Define the schema for child instance nodes in the global instance's graph. Each child is represented as a node with properties: name, type, description, path, metadata, last updated timestamp.
