# Registry Query API

The global instance should be able to list and query its registry of children. Support queries like: list all children, find children by type, find children whose description matches a query. This is used by the orchestrator for routing.
