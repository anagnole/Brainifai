# Instance Management UI

Provide a visual interface for creating, configuring, and deleting Brainifai instances — the same operations available via the CLI but in a web UI. Users should be able to choose instance types (from templates), set descriptions, and see the full list of existing instances with their status.
