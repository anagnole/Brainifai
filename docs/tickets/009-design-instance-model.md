# Design Instance Model

Design the data model for a Brainifai instance. Define what properties an instance has: name, type, description, parent reference, configured sources, path, metadata, etc. This model is the foundation for everything else.
