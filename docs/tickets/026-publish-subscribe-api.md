# Publish/Subscribe API

Build the API that instances use to interact with the event bus. Each instance should be able to: publish an event, subscribe to event types, handle incoming events with callbacks.
