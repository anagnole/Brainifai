# Instance Lifecycle Edge Cases

Handle edge cases in instance management: what happens when a child is deleted, when a project directory is moved or renamed, when two instances have conflicting names, or when the global instance is recreated.
