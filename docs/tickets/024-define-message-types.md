# Define Message Types

Define the core message types for the event bus: instance.registered (new instance announces itself), instance.updated (description or config changed), query.request (ask for information), query.response (respond with data), data.push (parent pushes ingested data to child).
