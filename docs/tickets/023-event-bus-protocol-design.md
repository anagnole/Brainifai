# Event Bus Protocol Design

Design the event bus protocol. Define message format (headers, payload, metadata), delivery guarantees (at-least-once vs best-effort), and transport mechanism. Consider: file-based, Unix sockets, or lightweight local message queue.
