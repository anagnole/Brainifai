# Instance Config Storage

Define where and how each instance stores its configuration (type, description, source subscriptions, custom settings). Likely a config file inside the .brainifai/ directory. Should be human-readable and editable.
