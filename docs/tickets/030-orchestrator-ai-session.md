# Orchestrator AI Session

Build the orchestrator as an AI session (Claude) that runs in the global instance. It has full knowledge of the instance tree — all child names, types, and descriptions. It uses this context to make routing decisions.
