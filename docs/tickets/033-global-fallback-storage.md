# Global Fallback Storage

Data that doesn't match any child instance's description stays in the global instance's graph. The global instance is the catch-all — nothing is lost, it just lives at the top level.
